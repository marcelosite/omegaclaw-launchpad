[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "omegaclaw-launchpad"
version = "0.3.0"
description = "Story-first Docker onboarding with a verified OmegaClaw proof and readable receipts."
readme = "README.md"
requires-python = ">=3.9"
license = {file = "LICENSE"}
authors = [{name = "OmegaClaw Launchpad contributors"}]
dependencies = []

[project.scripts]
omega-launchpad = "launchpad.cli:main"
omega-launchpad-mcp = "launchpad.studio.mcp_server:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
pythonpath = ["src"]
