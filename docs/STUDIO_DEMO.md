# Studio demonstration script

Run **on the user's computer or private VPS, from the repository root**:

```sh
scripts/launchpad-start.sh
```

The terminal visibly performs preflight, canonical example validation, and the real Docker/Test proof. Only then open `http://127.0.0.1:8765`.

Begin with the title and premise. Then read each split lesson from left to right: story first, matching OmegaClaw part second. Finish with the complete story-to-system map and copy the ready prompt into a coding agent. At the end show `examples/lighthouse-in-the-fog/AGENTS.md` and the private-workspace handoff.

Do not call a fixture receipt runtime evidence, skip a failed proof, expose port 8765, or connect MCP as a required step.
