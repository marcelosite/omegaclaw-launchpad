# Illustrative learning rule

## Rule R-001

When both `temperature_above_demo_threshold` and `vibration_above_demo_threshold` are true in this synthetic fixture, derive `manual_inspection_recommended`.

This is an illustrative rule for study. It has not been executed or validated in the real OmegaClaw runtime. It does not diagnose a fault, identify a cause, validate an external data source, or command a machine action.

The thresholds, asset identifier, and signals are fictional. A human remains responsible for deciding whether any real-world follow-up is appropriate.
