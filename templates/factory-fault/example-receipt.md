# Example receipt — fixture only

**Status:** fixture example, not a real OmegaClaw proof

## Input

The synthetic fixture supplied two fictional signals for `press-demo-07`: `temperature_above_demo_threshold` and `vibration_above_demo_threshold`.

## Illustrative rule scaffold

Rule R-001 illustrates `manual_inspection_recommended` only when both synthetic signals coexist. This scaffold has not been executed or validated in the real OmegaClaw runtime.

## Result

`manual_inspection_recommended`

This example does not claim a real equipment fault, a causal finding, external-data validation, an OmegaClaw runtime result, or authorization for an action.
