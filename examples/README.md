# OmegaClaw Launchpad examples

There is one canonical first example:

- [`lighthouse-in-the-fog/`](lighthouse-in-the-fog/) — receive, remember,
  verify, reason, and explain without taking an external action.

Start there. Copy it before changing facts or rules. The example receipt is a
fixture and never proves that OmegaClaw ran.
